{
  "name": "test-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "docker-build": "docker build -t test-nginx .", 
    "clean-dockers-linux": "docker ps -a | grep test-nginx | awk '{print $1}' | xargs -r docker rm -f ",
    "clean-dockers-mac": "docker ps -a | grep test-nginx | awk '{print $1}' | xargs docker rm -f ",
    "clean_dockers": "./clean_dockers.sh",
    "docker-start": "docker run -d -p 3000:80 --name test-nginx test-nginx",
    "start": "node ./test-server.js"
  },
  "dependencies": {
  }
}