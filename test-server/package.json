{
  "name": "test-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "docker-build": "docker build -t test-nginx .", 
    "clean_dockers": "docker ps -a | grep test-nginx | awk '{print $1}' | xargs docker rm -f ",
    "docker-start": "docker run -d -p 3000:80 --name test-nginx test-nginx",
    "start": "node ./bin/www"
  },
  "dependencies": {
    "body-parser": "~1.13.2",
    "cookie-parser": "~1.3.5",
    "debug": "~2.2.0",
    "express": "~4.13.1",
    "jade": "~1.11.0",
    "morgan": "~1.6.1",
    "serve-favicon": "~2.3.0"
  }
}